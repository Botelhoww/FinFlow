module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Certifique-se de que está buscando em todos os arquivos da pasta src
  theme: {
    extend: {}, // Aqui é possível customizar o tema
  },
  plugins: [], // Adicione plugins se necessário (não obrigatório)
};